export default class todo {
  constructor(title, project, details, dueDate, priority) {
    this.title = title;
    this.details = details;
    this.project = project;
    this.dueDate = dueDate;
    this.priority = priority;
  }
}
